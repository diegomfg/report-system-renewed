{{>header}}
<div class="bg-white p-4 flex flex-wrap flex-col justify-between leading-normal">
    <table class="table-fixed lg:w-1/3 mx-auto p-4">
        <thead>
            <tr>
                <th>Author</th>
                <th>Category</th>
                <th>Updated at</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{formatAuthor report.author}}</td>
                <td>{{report.category}}</td>
                <td>{{formatDate report.updated_at}}</td>
            </tr>
        </tbody>
    </table>
    <div class="w-full lg:w-1/3 max-w-1/2 min-w-xs mx-auto px-4">
        {{!-- @todo Style this page. Create a sidebar for the details and a content block for the content of the report
        --}}
        <h2
            class="block text-base xs:text-xl sm:text-xl md:text-xl lg:text-xl xl:text-4xl mt-4 underline decoration-solid">
            Title</h2>
        <h3 class="text-base xs:text-2xl sm:text-2xl md:text-2xl lg:text-2xl xl:text-2xl">{{report.title}}</h3>
        <h2
            class="block text-base xs:text-xl sm:text-xl md:text-xl lg:text-xl xl:text-xl mt-4 underline decoration-solid">
            Description</h2>
        <article class="prose lg:prose-xl">
            {{ report.body }}
        </article>
        {{#if (isAuthor user report)}}
        <a href="/reports/delete/{{report.id}}"
            class="bg-blue-500 hover:bg-blue-700 text-white p-2 font-bold border border-blue-700 rounded inline-block mt-3">Delete</a>
        <a href="/reports/update/{{report.id}}"
            class="bg-blue-500 hover:bg-blue-700 text-white p-2 font-bold border border-blue-700 rounded inline-block mt-3">Update</a>
        {{/if}}
    </div>

</div>
{{>footer}}